const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function main() { const now = new Date(); console.log('Now UTC:', now.toISOString()); const s = await prisma.session.findMany({ where: { status: 'SCHEDULED' }, orderBy: { startTime: 'asc' }, select: { topic: true, startTime: true, endTime: true, status: true } }); console.log('SCHEDULED sessions:', s.length); s.forEach(x => console.log(x.status, '|', x.startTime.toISOString(), '->', x.endTime.toISOString(), '| isPast:', x.endTime < now)); } main().catch(console.error).finally(() => prisma.$disconnect());
